
      <%= form_tag(top_path, :method => "get", class: "navbar-form", id: "top-form") do %>
    <div class="input-append">
      <%= text_field_tag :top, params[:top], class: "span2", placeholder: "Category name" %>

      <%= select_tag :category_id, options_from_collection_for_select(Category.all.order(:name), :id, :name) %>
      


      <br/>
<button name="button">Search</button>
    </div>
    <%end%>
